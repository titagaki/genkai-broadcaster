# CLAUDE.md

プロジェクトのルール・制約は `AGENTS.md` を正本とし、ここではそれを取り込むだけにする。
ルールを追加・変更するときは AGENTS.md 側を編集すること (両方に書いて食い違わせない)。

@AGENTS.md

## Claude Code 向けの補足

- 検証はビルドできないため、編集後に `python3` で Kotlin の括弧バランスを確認し、
  `grep -rn` で旧シンボルの参照残りをチェックする。
- UIの振る舞いを変えたら `docs/product/spec.md` と `docs/ui/current.md` を同じコミットで更新する。
- 画面追加・遷移変更時は `ui/AppRoot.kt` 冒頭の設計コメント (自前フラグ管理の理由) と矛盾しないか確認する。
- コミットメッセージは日本語で、要約1行 + 空行 + 変更点の箇条書き + 影響 (あれば)。
